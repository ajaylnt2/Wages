import googleLoginComponent from './googleLogin.component';

const googleLoginModule = angular.module('googleLoginElement', [])
  .component('googleLoginElement', googleLoginComponent);

export default googleLoginModule;
