import buttonElementComponent from './buttonElement.component';

const buttonElementModule = angular.module('buttonElement', [])
  .component('buttonElement', buttonElementComponent);

export default buttonElementModule;
